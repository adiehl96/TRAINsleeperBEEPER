<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".AlarmActivity"
    android:gravity="bottom|center_horizontal">

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/distanceText"
        android:textSize="@dimen/abc_text_size_headline_material"
        android:textColor="@color/abc_primary_text_material_dark"
        android:layout_marginBottom="100dp"
        android:textAlignment="center" />

    <Switch
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/activity_alarm_vibration_switch_text"
        android:id="@+id/switchVibration"
        android:switchPadding="@dimen/abc_button_padding_horizontal_material"
        android:checked="true"
        android:layout_marginBottom="@dimen/activity_vertical_margin" />

    <Switch
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/activity_alarm_ringtone_switch_text"
        android:id="@+id/switchRingtone"
        android:switchPadding="@dimen/abc_button_padding_horizontal_material"
        android:checked="true"
        android:layout_marginBottom="@dimen/activity_vertical_margin" />

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@android:string/cancel"
        android:onClick="cancelAlarm" />

</LinearLayout>
